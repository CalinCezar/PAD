# gRPC dependencies for Python broker
grpcio==1.60.0
grpcio-tools==1.60.0
protobuf==4.25.1

# Original dependencies (now using gRPC instead of raw sockets)
# - threading, sqlite3, json, xml.etree.ElementTree, datetime (standard library)